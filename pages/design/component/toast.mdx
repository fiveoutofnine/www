import { ExternalLink } from 'lucide-react';

import DesignLayout from '@/components/layouts/design';
import DesignComponentsDisplay from '@/components/pages/design/components-display';
import { Button, Toaster, useToast } from '@/components/ui';

# Toast

> `Toaster` extends and requires Radix UI's [`@radix-ui/react-toast`](https://www.radix-ui.com/docs/primitives/components/toast).

## Usage

WIP (will come after `CodeBlock` is implemented).

## Default

<DesignComponentsDisplay>
  <ToastButton title="Title" description="Swipe right to remove." children="Show toast" />
</DesignComponentsDisplay>

## Intent

<DesignComponentsDisplay className="grid-cols-2 md:grid-cols-5">
  <ToastButton intent="none" children="None" />
  <ToastButton intent="primary" children="Primary" />
  <ToastButton intent="fail" children="Fail" />
  <ToastButton intent="success" children="Success" />
  <ToastButton intent="warning" children="Warning" />
</DesignComponentsDisplay>

## With action

To add an action to the toast, pass in a `Button` element to the `action` prop. Note that `action` takes in any React node, but it is intended to be a `Button` element that triggers some relevant action.

<DesignComponentsDisplay>
  <ToastButton
    title="Contract deployed"
    description="Contract deployed to 0x00000000…."
    intent="success"
    action={
      <Button
        size="sm"
        intent="success"
        href="https://etherscan.io/tx/0x0100b7a91f27db8ea706a0f729bf0677e45a8647cf7dd25fc249dce3710bea81"
        rightIcon={<ExternalLink />}
        children="View"
        newTab
      />
    }
    children="Show toast"
  />
</DesignComponentsDisplay>

export const ToastButton = ({ title = 'Title', description = 'Swipe right to remove.', intent, action, ...rest }) => {
  const { toast } = useToast();

return <Button intent={intent} onClick={() => toast({ title, description, intent, action })} {...rest} />;
}

export default (props) => (
  <DesignLayout name="Toast" selected="/design/component/toast" {...props} />
);
